{
  "name": "@bank-ai/desktop-host",
  "version": "0.5.2",
  "description": "Локальный Windows host для Bank AI Word Add-in",
  "author": "Bank AI",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "dev": "npm run build && electron .",
    "prepare:grammar": "powershell -ExecutionPolicy Bypass -File scripts/prepare-grammar.ps1",
    "dist:win": "npm run prepare:grammar && electron-builder --win nsis --x64"
  },
  "dependencies": {
    "@bank-ai/local-runtime": "0.5.2",
    "dotenv": "^17.2.1",
    "office-addin-dev-certs": "^2.0.10"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "electron": "43.3.0",
    "electron-builder": "^26.0.12",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "build": {
    "appId": "kz.bankai.word",
    "productName": "Bank AI for Word",
    "artifactName": "BankAI-Setup-${version}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../addin/dist",
        "to": "addin"
      },
      {
        "from": "../addin/manifest.xml",
        "to": "manifest.xml"
      },
      {
        "from": "assets/icon.png",
        "to": "icon.png"
      },
      {
        "from": "assets/settings-preload.cjs",
        "to": "settings-preload.cjs"
      },
      {
        "from": "../../THIRD_PARTY_NOTICES.md",
        "to": "THIRD_PARTY_NOTICES.md"
      },
      {
        "from": "vendor/grammar",
        "to": "grammar",
        "filter": ["**/*", "!**/.downloads/**"]
      }
    ],
    "asarUnpack": [
      "node_modules/office-addin-dev-certs/**/*"
    ],
    "win": {
      "target": ["nsis"],
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": false,
      "createStartMenuShortcut": true,
      "runAfterFinish": true,
      "deleteAppDataOnUninstall": false,
      "include": "assets/installer.nsh"
    }
  }
}
