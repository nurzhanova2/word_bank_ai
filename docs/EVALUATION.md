# Evaluation

Deterministic Kazakh grammar evaluation remains local and uses the existing 200 annotated synthetic cases. It reports precision, recall, F1 and diagnostic metrics without calling an external provider. Live provider evaluation is opt-in and was not run in this environment.
